# Copy to .env for local development only. Never commit .env with real values.
# This file is safe to commit — placeholders only.

# Phase B+: Hermes HTTP endpoint (example only — use phone settings UI on device)
# HERMES_BASE_URL=https://hermes.your-tailnet.ts.net
# HERMES_TOKEN=your-token-here

# Do NOT put real secrets in VITE_* variables — they are bundled into the client.
